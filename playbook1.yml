- name: playbook1.yml
  hosts: all
  tasks:
    - copy:
       src: /tmp/newdata
       dest: /tmp/newcopied_data
    - yum:
       name: httpd
       state: latest
    - name: ensure apache is running

      service:
        name: httpd
        state: started

      

